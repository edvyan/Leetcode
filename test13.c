#include <stdio.h>

int linearSearch(int *arr, int i, int val)
{
    for (int k = 0; k < i; k++)
    {
        if (arr[k] = val)
        {
            return k;
        }
    }
    return -1;
}
int main(int argc, char *args[])
{

    return 0;
}
